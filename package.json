{
  "name": "pathniti",
  "version": "1.0.0",
  "private": true,
  "description": "PathNiti - One-Stop Personalized Career & Education Advisor for Indian Students",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "keep-alive": "node scripts/keep-alive.js",
    "keep-alive:prod": "node scripts/keep-alive.js --url https://pathniti.vercel.app"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "turbo": "^1.10.12",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "tailwindcss-animate": "^1.0.7"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@8.0.0"
}
